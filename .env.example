# Server
HOST=0.0.0.0
PORT=1337

# Secrets (generate with: openssl rand -base64 32)
APP_KEYS="key1,key2,key3,key4"
API_TOKEN_SALT=randomString
ADMIN_JWT_SECRET=randomString
TRANSFER_TOKEN_SALT=randomString
JWT_SECRET=randomString
ENCRYPTION_KEY=randomString

# Database — SQLite (producción)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# Database — PostgreSQL (alternativa)
# DATABASE_CLIENT=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=strapi_sga
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strong_password

# Database — MySQL (alternativa)
# DATABASE_CLIENT=mysql
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_NAME=strapi_sga
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strong_password
